#ifndef PACKET_PROCESSOR_H
#define PACKET_PROCESSOR_H

#include <stddef.h>
#include <stdint.h>

// void rx_consumer_init(void);
// void process_rx_packet(const uint8_t *data, size_t len, uint16_t transmitter_srdid);


// void process_rx_packet(const uint8_t *data, size_t len, uint16_t transmitter_srdid);

#endif